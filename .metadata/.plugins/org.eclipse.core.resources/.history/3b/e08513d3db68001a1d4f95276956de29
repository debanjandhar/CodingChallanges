import java.io.IOException;

public class MinimumSwaps2 {

	// Complete the minimumSwaps function below.
	static int minimumSwaps(int[] arr) {
		int numSwaps = 0;
		int pivot = 0;
		int index = 0;
		boolean swapFl = false;

		while (pivot < arr.length) {
			index = pivot + 1;
			if (arr[pivot] - (pivot + 1) > 0) {
				
			}

			index++;
		}

		return numSwaps;
	}
	
	/**
	 * @return -1 for left Shift, 1 for right shift and 0 for no shift
	 */
	private int mustShift(int ele, int position) {
		if (ele-position == 0) {
			return 0;
		}else if (ele-position) {
			
		}
	}

	public static void main(String[] args) throws IOException {

		System.out.println(minimumSwaps(new int[] { 1 }));
	}
}
